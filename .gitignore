# Dependencies
node_modules/
.pnp.*
.yarn/*

# Build output
.next/
out/
dist/
build/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Environment
.env
.env.*
!.env.example

# Supabase
supabase/.branches/
supabase/.temp/
supabase/.env

# Editor / OS
.DS_Store
.idea/
*.swp
.vscode/*
!.vscode/extensions.json

# Logs / coverage
*.log
npm-debug.log*
pnpm-debug.log*
coverage/

# Trillian (agent working files, per-repo config — never committed)
.trillian-repo.json
DECISIONS.md
*-HANDOFF.md
*-CHECKLIST.md

# Playwright MCP browser-session scratch
.playwright-mcp/

# Playwright e2e run artifacts (saved sessions, reports, traces)
e2e/.auth/
e2e/test-results/
e2e/playwright-report/

# Demo tree build output (scripts/demo-tree): fetched photos, the zipped
# GedZip, and the manifest that names them. The .ged itself is committed.
docs/reference/rootward-demo/media/
docs/reference/rootward-demo/media-manifest.json
docs/reference/rootward-demo/rootward-demo.gdz
